# 实施文档：业务层与查询体验

> **来源**：`doc/design/特性计划-业务层与查询体验.md`  
> **对照**：CRL.Simple（`CRL.Data` / `doc/业务Provider与Lambda查询指南.md`）  
> **目标**：把特性计划落成可编码的设计与任务拆分；**重点**为对象修改追踪、富仓储、Schema、查询缓存与 CASE API。  
> **原则**：SQL-first；扩展层接入；不替换内核；厚能力可选；**业务新 API 只进富仓储**（见 §0.1）。  
> **代码根目录**：`pure/src/adoext/richRepo/`（命名空间 `mooSQL.data`）。

修订：2026-08-11

---

## 0. 范围与交付物

| 优先级 | 交付 | 本文深度 |
|:------:|------|----------|
| P0 | Dirty Tracking（EntitySnapshot + **富仓储**脏更新） | **详细实现** |
| P0 | SooRichRepo / useRichRepo（富仓储） | **详细 API** |
| P0 | SchemaEnsure（EnsureSchema / SyncMode） | **详细实现** |
| P0 | SQLBuilder SELECT 结果缓存（`setCache` / SQLCmd 指纹） | **详细** → 专项计划 |
| P0 | UpsertOptions + **富仓储** InsertOrUpdate | 详细 |
| P1 | CASE / Window 一等 API（SQLBuilder → Clip） | 详细 |
| P1 | useTrans / Include / 分片注册 | 纲要（挂钩点） |

**不做（与特性计划一致）**：强制实体基类、CompileToSp 进内核、字段混淆、Remoting、用厚仓储替换 SQLBuilder、**把 EntityCache / SchemaEnsure / Dirty Tracking / UpsertOptions 下沉到 `SooRepository` 或更下层**。

### 0.1 落点边界（强制）

本特性按两类 API 落点，**禁止混淆**：

| 类型 | 含义 | 允许落点 | **禁止** |
|------|------|----------|----------|
| **SQL / 查询特性** | 一等 SQL 能力，不绑定业务仓储心智 | `SQLBuilder` / `StepBuilder` 优先；上层 `SQLClip`（及 LINQ）**借用**；可选 `useDDL().ensure` 等门面糖 | 在仓储层另起一套拼装 |
| **业务厚能力** | 字典缓存、结构对齐 UX、脏追踪、Repo Upsert 等 | **仅** `SooRichRepo` / `useRichRepo`（类型可放 `richRepo/`） | **不**扩展到 `SooRepository` / `useRepo`；不改薄仓默认行为 |

**业务厚能力清单（只进富仓储）**

- 脏字段追踪：`Track` / `UpdateDirty` / `UpdateAllColumns` / `autoTrackOnQuery` / `EntityTracking` 消费路径  
- 实体字典缓存：`AllCache` / `QueryFromCache` / `ClearCache` / `CacheQuery`  
- Schema UX：`EnsureSchema` / `PreviewSchema` / `SyncFields` / `SyncCaptions`（`SchemaEnsure` 静态门面可被 DDL 糖调用，但**仓储入口只有 RichRepo**）  
- `UpsertOptions` + `InsertOrUpdate`  

**SQL / 查询特性清单（可进 Builder / Clip，不进薄仓业务 API）**

- CASE / Window（`CaseBuilder` / `WindowBuilder`）  
- SELECT 结果缓存：`setCache(key,ttl)` / `setCache(ttl)`+SQLCmd 指纹（与 EntityCache 分离；见专项计划）  
- 既有 nav、`useTrans` 等门面糖（不替代 RichRepo）  

**允许的下层挂钩（非对外 API）**：`EntityTranslator.prepareUpdateMembers` 等翻译能力可供富仓储调用；**不得**因此在 `SooRepository` 上暴露 Tracking / Upsert / Schema / EntityCache API。

`useRepo` 保持薄 CRUD；需要上述厚能力时显式 `useRichRepo`。

---

## 1. 现状基线（mooSQL）

| 能力 | 现状 | 关键路径 |
|------|------|----------|
| 薄仓储 | ✅ `SooRepository` / `useRepo` | `pure/src/adoext/repository/` |
| 实体更新 | 全列 `prepareUpdate`（可 `includeUpdate`/`ignoreUpdate`） | `EntityTranslator.cs` |
| 脏字段 | ✅ `EntitySnapshot` / `EntityChangeBag` / `EntityTracking`（仅富仓储消费） | `adoext/richRepo/tracking/` |
| 结果缓存 | ✅ SQLBuilder `setCache(key\|ttl)` + SQLCmd 指纹 + `ISooCache`（R0–R7 已完成；**不**提供 Clip `.expire`） | `ado/cache/`、`StepBuilder.resultCache`、专项计划 |
| 实体字典缓存 | ✅ RichRepo `AllCache` / `QueryFromCache` / `ClearCache` | `adoext/richRepo/EntityCacheStore.cs` |
| MERGE / Upsert SQL | ✅ `MergeIntoBuilder` + InsertOrUpdate IR | `ado/builder/SQLKit/` |
| Repo UpsertOptions | ✅ **仅** `SooRichRepo.InsertOrUpdate` + `UpsertOptions`（优先方言 upsert，不支持则先查后写） | `adoext/richRepo/` |
| CASE / Window IR | ✅ `CaseWord` / `WindowOverClause`；Ext LINQ 分析函数已有 | `ado/SQL/`、`ext/src/linq` |
| Clip CASE/Window | ✅ CASE/Window 薄封装借用 SQLBuilder；Lambda 糖另议 | `adoext/clip/SQLClip.case.cs` / `SQLClip.window.cs` |
| 导航加载 | ✅ SQLBuilder `includeHis`/`includeNav`；Repo/Clip `Include` 糖复用 Nav | `adoext/nav/` |
| 事务 | ✅ `LiveTransaction` / `useWork` / **`db.useTrans`** 同库糖 | `ado/data/context/`、`utils/door/` |
| Schema | ✅ `EnsureSchema` / `PreviewSchema` / `SyncMode`（RichRepo + `useDDL().ensure`） | `adoext/richRepo/schema/` |

---

## 2. CRL → mooSQL 映射（采纳 / 改造）

| CRL | mooSQL 落点 | 决策 |
|-----|-------------|------|
| `IModel.Changes` + `Change()` / `Cumulation()` | `EntityChangeBag`（可选）+ 扩展方法 | **采纳**手动标记；不强制继承；**仅富仓储消费** |
| `ModelTracking`（hash 快照） | `EntitySnapshot`（**原始值**快照） | **改造**：不用 `GetHashCode`；**仅富仓储** |
| 无 Changes 则全列更新 | 富仓储 `Update` 脏列；`UpdateAllColumns` 逃生；薄仓仍全列 | **改造**：厚路径更安全；薄仓行为不变 |
| `ProviderOrigin` / `BaseProvider` | `SooRepository`（薄）+ `SooRichRepo`（厚） | **分层**：厚可选；厚 API 不回灌薄仓 |
| `AllCache` / `QueryFromCache` / `CacheQuery` | RichRepo 实体字典缓存 | **采纳**；不进 `useRepo` |
| `Expire(minutes)` 查询缓存 | SQLBuilder `setCache(key\|ttl)`（**不**提供 Clip `.expire`） | **改造**；属 SQL/结果缓存特性，≠ EntityCache |
| `InsertOrUpdateOption` | `UpsertOptions` + **富仓储** API | **采纳**；不进薄仓 |
| `PackageTrans` | `db.useTrans(...)` | **采纳**糖；同库 |
| `CreateCase` / `CreateWindowFunc` | SQLBuilder `caseWhen` / `window`→ Clip 借用 | **采纳**；SQL 特性，先 Builder |
| `Include` + Relation | Repo/Clip `Include` → 现有 nav | **复用** `NavQueryGuide`，不重造 |
| `SyncTableFields`（可 DROP） | `EnsureSchema(SyncMode)` + 双闸 | **改造**：默认只增不删；入口以 RichRepo 为主 |

---

## 3. P0-1 对象修改追踪（Dirty Tracking）— 详细实现

### 3.1 设计目标

1. `Update(entity)` 只 `SET` 变更列（+ PK WHERE）。  
2. POCO 友好：不强制实体基类、不做动态代理。  
3. 提供三条路径（按推荐顺序）：  
   - **A. 快照追踪**（主推，查询后自动或显式开始）  
   - **B. 手动标记**（`MarkDirty` / `Change`，对标 CRL `Change`）  
   - **C. 全量逃生**（`UpdateAllColumns`）  
4. 最终仍落到 `SQLBuilder.update().set(...).where(...)` / `EntityTranslator`。

### 3.2 类型与文件布局

**代码根目录**：`pure/src/adoext/richRepo/`

```text
pure/src/adoext/richRepo/
  tracking/
    ITrackedEntity.cs          # 可选接口（不强制实现）
    EntityChangeBag.cs        # 手动脏字段袋（类 CRL Changes）
    EntitySnapshot.cs          # 原始值快照 + Diff
    TrackingOptions.cs         # 排除列、比较器、大列策略
    EntityTracking.cs          # 静态门面：Begin / GetDiff / Attach
    EntityTrackingExtensions.cs # MarkDirty / Cumulate 扩展方法

挂钩（仅增强 Translator，**不修改** `SooRepository`）：
  EntityTranslator.prepareUpdateMembers / 主键不受 includeUpdate 过滤
  全部业务 API 落在 `SooRichRepo`（`new Update` / UpdateDirty / Track / …）
```

命名空间建议：`mooSQL.data`。

### 3.3 核心类型设计

#### 3.3.1 `EntityChangeBag`（手动标记，对标 CRL `IModel.Changes`）

```csharp
namespace mooSQL.data
{
    /// <summary>
    /// 实体脏字段袋。可不挂在实体上，由 Tracking 上下文持有。
    /// </summary>
    public sealed class EntityChangeBag
    {
        // propertyName → value；键以 '$' 前缀表示累加/表达式更新（对标 CRL Cumulation）
        readonly Dictionary<string, object> _changes = new(StringComparer.Ordinal);

        public void Set(string memberName, object value);
        public void SetCumulation(string memberName, object delta); // → "$Email"
        public bool IsModified { get; }
        public IReadOnlyDictionary<string, object> GetChanges();
        public void Clear();
    }
}
```

扩展方法（对标 `CRL.Data.ExtensionMethod.Change`）：

```csharp
// 读取当前属性值写入 bag（需外部 bag 或 ITrackedEntity）
entity.MarkDirty(x => x.Email);
entity.MarkDirty(x => x.Email, "n@x.com");   // 赋值 + 标记
entity.Cumulate(x => x.Score, 1);            // Score = Score + 1
```

**约束**：手动路径不要求继承；`EntityChangeBag` 可由：

- `ConditionalWeakTable<object, EntityChangeBag>` 附着，或  
- 可选 `ITrackedEntity.ChangeBag` 属性。

推荐 **WeakTable**，保持 POCO 干净。

#### 3.3.2 `EntitySnapshot`（主推，改造 CRL `ModelTracking`）

CRL 用 `GetHashCode` 比对，存在碰撞风险。mooSQL **存原始值**，用值相等比较。

```csharp
public sealed class EntitySnapshot
{
    readonly object _entity;
    readonly Type _entityType;
    readonly Dictionary<string, object> _origin; // PropertyName → boxed original
    readonly TrackingOptions _options;

    public EntitySnapshot(object entity, EntityInfo meta, TrackingOptions options = null);

    /// <summary>与当前实体属性逐字段比较，返回需更新的列字典（不含 PK）。</summary>
    public Dictionary<string, object> GetDiff();

    /// <summary>用当前值刷新快照（Update 成功后调用）。</summary>
    public void AcceptChanges();

    public bool HasChanges => GetDiff().Count > 0;
}
```

**比较规则**

| 类型 | 比较 |
|------|------|
| 值类型 / string | `Equals` / `string.Equals` |
| `byte[]` / 大 BLOB | 默认 **排除**（`TrackingOptions.ExcludeLargeTypes`）；或长度+可选深度比较 |
| 引用类型（导航） | 默认跳过（非列映射属性） |
| `null` | 与 origin null 相等则非脏 |

**构造时只快照已映射列**（`EntityInfo.Columns`），跳过 `Ignore` / 非持久化 / 导航属性。

#### 3.3.3 `TrackingOptions`

```csharp
public sealed class TrackingOptions
{
    /// <summary>永不参与脏检测的属性名（BLOB/JSON/审计列等）。</summary>
    public HashSet<string> ExcludeMembers { get; } = new(StringComparer.Ordinal);

    /// <summary>默认排除 byte[] / Stream 等大列。</summary>
    public bool ExcludeLargeTypes { get; set; } = true;

    /// <summary>无脏字段时：Throw | NoOp | FallBackAllColumns（默认 NoOp）。</summary>
    public DirtyEmptyBehavior EmptyBehavior { get; set; } = DirtyEmptyBehavior.NoOp;
}

public enum DirtyEmptyBehavior { NoOp, Throw, FallBackAllColumns }
```

**与 CRL 差异**：CRL 在无 Changes/Tracking 时静默全列更新；mooSQL **默认 NoOp**（返回 0 / false），全量必须显式 `UpdateAllColumns`，避免误伤。

#### 3.3.4 `EntityTracking` 门面

```csharp
public static class EntityTracking
{
    // 开始追踪（查询后或手动）
    public static EntitySnapshot Begin<T>(T entity, EntityInfo meta = null, TrackingOptions opt = null);

    public static void BeginRange<T>(IEnumerable<T> entities, ...);

    // 取 Diff：优先 Snapshot；其次 ChangeBag；皆无则按 EmptyBehavior
    public static IReadOnlyDictionary<string, object> GetDirtyMembers(object entity);

    public static bool IsTracked(object entity);
    public static void Detach(object entity);
    public static void AcceptChanges(object entity);
}
```

内部用 `ConditionalWeakTable<object, TrackingState>`：

```csharp
sealed class TrackingState
{
    public EntitySnapshot Snapshot;
    public EntityChangeBag Bag;
}
```

优先级：`Bag` 非空 → 用 Bag；否则 Snapshot Diff；否则 EmptyBehavior。

### 3.4 与 EntityTranslator 挂钩

在 `prepareUpdate` 增加「列白名单」入口（已有 `includeUpdateFields`，复用即可）：

```text
SooRepository.Update(entity)
  → dirty = EntityTracking.GetDirtyMembers(entity)
  → 若 dirty 为空 → EmptyBehavior
  → translator.includeUpdate(dirty.Keys).prepareUpdate(...)
  → 成功后 EntityTracking.AcceptChanges(entity)
  → 可选：RichRepo 写路径失效/补丁缓存
```

新增重载（或内部方法）：

```csharp
StatusResult prepareUpdate(
    SQLBuilder builder, object entity, Type entityType,
    IReadOnlyCollection<string> onlyMembers,  // null = 现有全列逻辑
    EntityInfo en = null, Func<string> loadName = null);
```

**Cumulation**：`$Member` → `builder.set(col, rawExpression)`，如 `Score = Score + @p`，方言由 Dialect 拼表达式（对标 CRL `GetFieldConcat`）。首期可仅支持数值 `+`；字符串 concat 按方言跟进。

### 3.5 SooRichRepo API（独立于 SooRepository）

`SooRepository` **保持不动**。以下 API 仅在 `SooRichRepo<T>` / `useRichRepo`：

```csharp
var repo = db.useRichRepo<User>().autoTrackOnQuery();

repo.Track(entity);
repo.Update(entity);           // 已追踪 → 脏更新；否则 base 全列
repo.UpdateDirty(entity);
repo.UpdateAllColumns(entity); // 显式全列 → base.Update
```

**兼容策略（重要）**

| 阶段 | `Update(entity)` 行为 |
|------|------------------------|
| 引入期（默认） | 已追踪 → 脏更新；未追踪 → **全列**（与现网兼容） |
| 文档推荐 | `autoTrackOnQuery(true)` + 业务统一 `Update` |
| 可选严格模式 | `TrackingMode.RequireTracked` → 未追踪抛异常 |

批量：`UpdateRange` 逐条走同一策略；大批量显式 `set` / `Update(columns, where)` 不走追踪。

### 3.6 查询自动追踪

仅在 **`SooRichRepo`** 覆盖的查询物化路径上自动 `Begin`：

- `GetById` / `GetByIds`（含 `params` / `IEnumerable`）  
- `GetList()` / `GetList(top)` / `GetList(Expression)` / `GetList(Action<SQLBuilder>)` / `GetList(Action<SQLClip,T>)` / `GetList(QueryPara)`  
- `GetPageList(...)` 各重载（对 `PageOutput.Data` / `Rows` 追踪）  

```csharp
if (_autoTrack) EntityTracking.BeginRange(list, En, _trackingOptions);
```

- **`SooRepository` / `useRepo`：不挂 autoTrack，不改查询语义。**  
- Clip / Fast LINQ / Ext LINQ：**不默认**自动追踪；需要时由业务侧 `richRepo.Track(list)` 显式接入（Clip `.track()` 非本迭代必做）。

### 3.7 验收用例

| # | 场景 | 期望 |
|---|------|------|
| 1 | `useRichRepo` GetById → 改 Email → Update | SQL 仅 `SET Email=... WHERE Id=...` |
| 2 | 未改字段 UpdateDirty | 0 行 / false（NoOp） |
| 3 | UpdateAllColumns | 全可更新列 |
| 4 | MarkDirty + Cumulate | `Score = Score + @p` |
| 5 | 排除 byte[] | 大列不进 SET |
| 6 | `useRepo.Update` 行为与改造前一致 | 薄仓无追踪 API、无默认脏更新 |
| 7 | 基准 TestFast | 无显著回退 |

### 3.8 实现任务拆分（Tracking）

| 序号 | 任务 | 预估 | 状态 |
|:----:|------|:----:|:----:|
| T1 | `EntityChangeBag` + MarkDirty/Cumulate 扩展 | 0.5d | ✅ |
| T2 | `EntitySnapshot` + 值比较 + Options | 1d | ✅ |
| T3 | `EntityTracking` WeakTable 门面 | 0.5d | ✅ |
| T4 | `EntityTranslator` onlyMembers / cumulation（挂钩，非薄仓 API） | 1d | ✅ |
| T5 | **`SooRichRepo`** UpdateDirty / UpdateAllColumns / Track / autoTrack（**不改** `SooRepository`） | 1d | ✅ |
| T6 | 单元测试 + 打印 SQL 断言 | 1d | ✅ |

---

## 4. P0-2 富仓储 SooRichRepo — API 设计落实

### 4.1 定位（对标 CRL BaseProvider）

```text
SooRepository<T>              ← 薄 CRUD（保持默认；不挂厚 API）
SooRichRepo<T>                ← 厚业务层（独立类型，组合薄仓转发 CRUD）
```

- **不强制**业务继承 RichRepo；`useRepo` 仍是默认。  
- 厚能力 **只**挂在 RichRepo（见 §0.1）；**不**继承 `SooRepository`；代码落在 `pure/src/adoext/richRepo/`（命名空间 `mooSQL.data`）；入口 `DBInstance.useRichRepo`。
- 需要薄仓实例时用 `rich.Thin`。

### 4.2 类型与入口

```csharp
// pure/src/adoext/richRepo/SooRichRepo.cs
public class SooRichRepo<T> where T : class, new()  // 独立类型，不继承 SooRepository
{
    public SooRepository<T> Thin { get; }           // 内部组合的薄仓

    // —— 实体字典缓存（对标 BaseProvider）——
    public IEnumerable<T> AllCache { get; }
    public void ClearCache();
    protected virtual SQLBuilder CacheQuery();          // 定义预热范围，默认全表
    public List<T> QueryFromCache(Expression<Func<T, bool>> pred);
    public T QueryItemFromCache(object pk);
    public T QueryItemFromCache(Expression<Func<T, bool>> pred);

    // —— Schema UX（详见 §4.6）——
    public SchemaEnsureResult EnsureSchema(SyncMode mode = SyncMode.AddMissingColumns);
    public SchemaEnsureResult EnsureSchema(SchemaEnsureOptions options);
    public IReadOnlyList<string> PreviewSchema(SyncMode mode = SyncMode.AddMissingColumns);
    public void SyncFields(SyncMode mode = SyncMode.AddMissingColumns);
    public void SyncCaptions();

    // —— Upsert ——
    public int InsertOrUpdate(T entity, UpsertOptions options = null);
    public int InsertOrUpdate(IEnumerable<T> entities, UpsertOptions options = null);
}

// Door
DBInstance.useRichRepo<T>()
MooClient / DBClientFactory 同步工厂方法
```

> `SyncMode` / `SchemaEnsureOptions` 定义见 **§4.6**，勿在业务代码直接传 `DMLOption.IsDropColumn`。

### 4.3 实体字典缓存（EntityCache）实现要点

与 **结果缓存（ResultCache）** 严格分离：

| | ResultCache | EntityCache（RichRepo） |
|--|-------------|------------------------|
| 键 | SQL + 参数 + DB 名（见 §5） | `Type.FullName + "|" + DatabaseName` |
| 值 | 查询结果集副本 | `Dictionary<string, T>`（PK → 实体） |
| 过滤 | SQL 侧 | **内存** `Compile().Invoke` / 简易表达式 |
| 失效 | TTL；写可选 Remove | 写后 Clear 或按 PK Patch |
| 分页 | **禁止缓存**（同 CRL） | 内存分页可选 |

```csharp
// 内部
sealed class EntityCacheStore
{
    // 复用 ISooCache 或独立 ConcurrentDictionary
    Dictionary<string, T> Warm(SooRichRepo<T> repo);
}

protected virtual SQLBuilder CacheQuery()
{
    // 默认：select * from T；子类可 where Status=1 缩小字典
    return DBLive.useSQL().select("*").from(En.DbTableName);
}
```

**TTL**：RichRepo 可配置 `EntityCacheSeconds`（默认 300，对标 CRL 5 分钟）；到期后下次访问重建。  
**写后策略（首期）**：`Insert/Update/Delete/InsertOrUpdate` → `ClearCache()`（简单正确）。  
**增强（二期）**：按 PK Patch（对标 CRL `UpdateCacheItem`）。

**主键等值短路**：`QueryFromCache(x => x.Id == id)` 走字典 TryGetValue（CRL 已有优化，应保留）。

### 4.4 富仓储完整 API 清单（落实）

通过组合薄仓转发 CRUD（`Thin`），并额外保证暴露：

```text
# 缓存
AllCache / ClearCache / CacheQuery / QueryFromCache / QueryItemFromCache

# Schema
EnsureSchema / PreviewSchema / SyncFields / SyncCaptions → §4.6 SchemaEnsure

# Upsert（仅富仓储）
InsertOrUpdate(entity|list, UpsertOptions)

# 追踪（仅富仓储；不改 SooRepository）
Update / UpdateDirty / UpdateAllColumns / Track / autoTrackOnQuery
```

业务子类推荐写法：

```csharp
public class SysDictBiz : SooRichRepo<SysDict>
{
    protected override SQLBuilder CacheQuery()
        => base.CacheQuery().where("Enabled", 1);

    public string GetName(string code)
        => QueryItemFromCache(x => x.Code == code)?.Name;
}

// 使用
var dict = db.useRichRepo<SysDict>(); // 或 new SysDictBiz().Attach(db)
```

工厂：`useRichRepo<T>()` 返回 `SooRichRepo<T>`；若注册了自定义类型可通过 `IRichRepoFactory` 解析（二期）。

### 4.5 实现任务拆分（SooRichRepo）

| 序号 | 任务 | 预估 | 状态 |
|:----:|------|:----:|:----:|
| B1 | `SooRichRepo` 骨架 + `useRichRepo` | 0.5d | ✅ |
| B2 | EntityCache Warm / QueryFromCache / ClearCache | 1.5d | ✅ |
| B3 | 写路径 ClearCache 挂钩 | 0.5d | ✅ |
| B4 | EnsureSchema → 见 §4.6 / S1–S5 | 并入 Schema | ✅ |
| B5 | 示例与文档片段 | 0.5d | ✅ 见 [`doc/docs/业务仓储与缓存指南.md`](../../docs/业务仓储与缓存指南.md) |

### 4.6 Schema 能力（EnsureSchema）— 详细实现

对标 CRL `CreateTable` / `SyncTableFields` / `UpdateTableFiledComments`，但 **默认绝不 DROP**（CRL `SyncTableFields` 会删多余列，不可照搬）。

#### 4.6.1 现状与缺口

| 能力 | 现状 | 说明 |
|------|------|------|
| `DDLBuilder.doInitTable` | ✅ | 无表则建；有表则 `doAlterTableLogic` |
| `DMLOption.IsDropColumn` | ✅ 默认 `false` | 仅显式 true 才删多余列 |
| 列类型修正 / PK / 注释 | ✅ 部分在 alter 路径 | 建表带 caption；alter 会补列注释 |
| `DBTableCreator` | 🟡 仅 create-if-missing | 有表即 return，补列逻辑死代码；**Ensure 勿依赖它** |
| `SyncMode` / `EnsureSchema` | ✅ | `richRepo/schema/` + RichRepo / `useDDL().ensure` |
| 生产环境开关 | ✅ | `SchemaEnsure.DefaultAllowSchemaSync` + `client.configureSchema` / Options 双闸 |

#### 4.6.2 文件布局（均在 `richRepo`）

```text
pure/src/adoext/richRepo/
  schema/
    SyncMode.cs
    SchemaEnsureOptions.cs
    SchemaEnsure.cs              # 静态/实例门面，包装 DDLBuilder
    SchemaEnsureExtensions.cs    # db.useDDL().ensure<T>() / ensure(...)

复用（不迁目录）：
  pure/src/ado/DDL/DDLBuilder.entity.cs   # doInitTable / toInitTableList / doAlterTableLogic
  pure/src/ado/DDL/DMLOption.cs           # IsDropColumn 映射目标
```

#### 4.6.3 `SyncMode` 与选项

```csharp
namespace mooSQL.data
{
    public enum SyncMode
    {
        /// <summary>仅当表不存在时 CREATE（含索引/注释）；已存在则跳过。</summary>
        CreateIfMissing = 0,

        /// <summary>推荐默认：无表则建；有表则只增加缺失列（及安全范围内的类型/PK/注释对齐）。不 DROP。</summary>
        AddMissingColumns = 1,

        /// <summary>在 AddMissingColumns 基础上刷新表/列注释。</summary>
        SyncCaptions = 2,

        /// <summary>危险：删除实体中不存在的 DB 列。须 Options.AllowDropColumn=true。</summary>
        AddAndDropExtraColumns = 9
    }

    public sealed class SchemaEnsureOptions
    {
        public SyncMode Mode { get; set; } = SyncMode.AddMissingColumns;

        /// <summary>生产可全局 false；为 false 时任何 Ensure 直接抛或 NoOp。</summary>
        public bool AllowSchemaSync { get; set; } = true;

        /// <summary>仅 Mode=AddAndDropExtraColumns 时生效；默认 false，双闸防误删。</summary>
        public bool AllowDropColumn { get; set; } = false;

        /// <summary>true → 调 toInitTableList，不执行。</summary>
        public bool PreviewOnly { get; set; } = false;

        /// <summary>分片物理表名；空则用实体默认表名。</summary>
        public string PhysicalTableName { get; set; }

        /// <summary>Preview 或执行后回填的 SQL 列表。</summary>
        public List<string> ScriptsOut { get; } = new();
    }
}
```

**双闸规则**：`AddAndDropExtraColumns` 且 `AllowDropColumn==true` 才映射 `DMLOption.IsDropColumn=true`；缺任一条件 → 按 `AddMissingColumns` 降级并打日志/警告。

#### 4.6.4 门面 API

```csharp
public static class SchemaEnsure
{
    public static SchemaEnsureResult Ensure<T>(DBInstance db, SchemaEnsureOptions opt = null);
    public static SchemaEnsureResult Ensure(DBInstance db, Type type, SchemaEnsureOptions opt = null);
    public static SchemaEnsureResult Ensure(DBInstance db, IEnumerable<Type> types, SchemaEnsureOptions opt = null);

    // Preview
    public static IReadOnlyList<string> Preview<T>(DBInstance db, SyncMode mode = SyncMode.AddMissingColumns);
}

public sealed class SchemaEnsureResult
{
    public bool Success { get; set; }
    public string Message { get; set; }
    public IReadOnlyList<string> Scripts { get; set; }
}
```

**RichRepo / DDL 入口**：

```csharp
// SooRichRepo<T>
public SchemaEnsureResult EnsureSchema(SyncMode mode = SyncMode.AddMissingColumns);
public SchemaEnsureResult EnsureSchema(SchemaEnsureOptions options);
public IReadOnlyList<string> PreviewSchema(SyncMode mode = SyncMode.AddMissingColumns);
public void SyncFields(SyncMode mode = SyncMode.AddMissingColumns); // → EnsureSchema 别名
public void SyncCaptions(); // → Mode=SyncCaptions

// Door（可选糖）
db.useDDL().ensure<T>(SyncMode.AddMissingColumns);
db.useDDL().ensure<T>(new SchemaEnsureOptions { PreviewOnly = true });
db.useRichRepo<User>().EnsureSchema();
```

对标 CRL Provider：

| CRL | mooSQL richRepo |
|-----|-----------------|
| `CreateTable()` | `EnsureSchema(CreateIfMissing)` |
| `SyncTableFields()` | `EnsureSchema(AddMissingColumns)`；DROP 须显式危险模式 |
| `UpdateTableFiledComments()` | `SyncCaptions()` / `SyncMode.SyncCaptions` |
| `CreateTableIndex()` | 含于 `doInitTable` 建表分支的 `doAddIndex`；缺索引补齐可作为二期 |

#### 4.6.5 执行映射

```text
EnsureSchema(opt)
  → if !AllowSchemaSync → fail / no-op（可配置）
  → map SyncMode → DMLOption:
       CreateIfMissing      → 仅 hasTable? skip : doInitTable(无 alter)
       AddMissingColumns    → doInitTable(IsDropColumn=false)
       SyncCaptions         → doInitTable(false) + 注释刷新路径
       AddAndDropExtra…     → 双闸通过则 IsDropColumn=true
  → PreviewOnly ? toInitTableList : doInitTable
  → ScriptsOut 回填
```

`CreateIfMissing` 实现注意：若表已存在应 **跳过 alter**（与 `AddMissingColumns` 区分）；可先 `hasTable` 再分支。

分片表：`PhysicalTableName` 非空时，走带物理表名的 init/create 路径（对接现有分片 DDL / `ShardDdlHelper`；若仅 create-only，文档标明首期限制）。

#### 4.6.6 安全与环境

| 场景 | 策略 |
|------|------|
| 开发 / 测试 | 默认 `AddMissingColumns` |
| 生产 | `AllowSchemaSync=false` 或仅 Preview；禁止 Drop |
| CI | `PreviewOnly` 断言无意外 DROP 脚本 |
| 多库 | 尊重 `entity.DBPosition`（`doInitTable` 已有跳过逻辑） |

客户端级开关建议：

```csharp
client.configureSchema(c => {
    c.AllowSchemaSync = env.IsDevelopment();
    c.AllowDropColumn = false;
});
```

（配置挂 `MooClient` / `BaseClientBuilder`，`SchemaEnsure` 读取。）

#### 4.6.7 验收用例

| # | 场景 | 期望 |
|---|------|------|
| 1 | 无表 EnsureSchema | CREATE + 索引 + 注释 |
| 2 | 有表缺列 | 仅 ADD COLUMN（+ caption） |
| 3 | 有表多列、默认 Mode | **不** DROP |
| 4 | Drop 模式但未 AllowDropColumn | 降级为只增；告警 |
| 5 | PreviewOnly | 返回 SQL，库不变 |
| 6 | SyncCaptions | 注释更新，结构不删列 |

#### 4.6.8 任务拆分（Schema）

| 序号 | 任务 | 预估 | 状态 |
|:----:|------|:----:|:----:|
| S1 | `SyncMode` + `SchemaEnsureOptions` + Result | 0.5d | ✅ |
| S2 | `SchemaEnsure` 包装 doInitTable / toInitTableList / 双闸 | 1d | ✅ |
| S3 | RichRepo EnsureSchema / Preview / SyncCaptions | 0.5d | ✅ |
| S4 | `useDDL().ensure<T>` 扩展 + Client 开关 | 0.5d | ✅ |
| S5 | CreateIfMissing 分支 + 单测 / Preview 断言 | 1d | ✅ |

---

## 5. P0-3 查询结果缓存（SQLBuilder SELECT）

> **详细实施计划（取代原 `.expire` 方案）**：[`SQLBuilder-Select结果缓存-实施计划.md`](./SQLBuilder-Select结果缓存-实施计划.md)  
> **口径变更**：不新增 `.expire`；不向 Clip/LINQ 下沉缓存管理 API；显式 key 用既有 `setCache(key,ttl)`；无 key 用 SQLCmd 指纹；**只缓存 SELECT 结果**。RichRepo EntityCache 仍只在富仓储。

### 5.1 目标

SQLBuilder ResultCache：

- `setCache(key, timeoutSeconds)`（显式 key）+ 修复写路径  
- `setCache(timeoutSeconds)`（无外界 key → SQLCmd 指纹）  
- 底层走 `ISooCache`；文档区分 **结果缓存** vs **实体字典缓存**

### 5.2 Key 规则

```text
ResultCacheKey = "RC:" + Hash(
    databaseName + "\n" +
    sqlText + "\n" +
    normalizedParams
)
```

- 显式 `useCache("myKey", ttl)` 时：`RC:USER:myKey`（仍建议附带 DB 名防串库）。  
- 分页查询（`setPage` / `skipTake` 且 pageIndex>0 或 skip>0）：**默认不缓存**（与 CRL 一致）；若强制缓存须显式 key + 文档警告。  
- 事务开启中：默认跳过读缓存与写缓存（避免脏读进缓存）。

### 5.3 API（摘要）

```csharp
kit.setCache("report:daily", 300);  // 显式 key
kit.setCache(300);                  // 无外界 key → SQLCmd 指纹（仅 Select）
// 不提供 kit.expire / clip.expire
```

完整流程、指纹规则、事务/分页策略、任务 R0–R7 见专项计划。

### 5.4 与 SooRichRepo 的关系

- `setCache` = ResultCache，缓存 **本次 SELECT 结果**。  
- `QueryFromCache` = EntityCache，缓存 **整表/子集字典**（仅富仓储）。  
- 二者不要混用同一文档示例而不加说明。

### 5.5 任务

以专项计划 **R0–R7** 为准。原 C1–C5（`.expire` / Clip / LINQ）**取消**。

---

## 6. P0-4 UpsertOptions + 富仓储 InsertOrUpdate

文件：`pure/src/adoext/richRepo/UpsertOptions.cs`（选项类型可被调用方引用；**对外 InsertOrUpdate API 仅 `SooRichRepo`**，不进 `SooRepository`）。

### 6.1 选项对象（对标 CRL `InsertOrUpdateOption`，跨方言）

```csharp
public sealed class UpsertOptions
{
    /// <summary>唯一判定列；空则用 PK。</summary>
    public List<string> ConstraintMembers { get; } = new();

    /// <summary>匹配后要更新的列；空则更新除约束列外全部可更新列。</summary>
    public List<string> UpdateMembers { get; } = new();

    /// <summary>存在则跳过更新（仅插入）。</summary>
    public bool IfExistsSkipUpdate { get; set; }

    /// <summary>批量切片大小；0 = 方言默认。</summary>
    public int BatchSize { get; set; } = 500;

    /// <summary>调试：最后生成的 SQL（可选 out）。</summary>
    public string SqlOut { get; set; }
}
```

### 6.2 方言策略

| 方言 | 实现 |
|------|------|
| SQL Server / Oracle / PostgreSQL / Oscar / DM | 现有 MERGE 管线 |
| MySQL | ON DUPLICATE KEY UPDATE |
| 其它 | 降级「先查后写」（`SqlOut` 带 `select-write:`；需文档标注） |

入口：

```csharp
db.useRichRepo<User>().InsertOrUpdate(entity, options);
db.useRichRepo<User>().InsertOrUpdate(list, options);  // BatchSize=逐条切片，非多行 SQL
// 禁止：useRepo<T>().InsertOrUpdate(...)  — 薄仓不扩展此 API
```

内部复用 `MergeIntoBuilder` / `InsertOrUpdateSentence`，由 `EntityTranslator` 填列。

### 6.3 任务

| 序号 | 任务 | 预估 | 状态 |
|:----:|------|:----:|:----:|
| U1 | `UpsertOptions` + **SooRichRepo** API | 0.5d | ✅ |
| U2 | 接 MERGE / 方言 upsert，单测（SQLite 降级先查后写；MySQL/MSSQL 原生路径） | 1.5d | ✅ |
| U3 | 批量切片 | 0.5d | ✅ |

---

## 7. P1 CASE / Window 一等 API

### 7.1 原则

**SQLBuilder 先落地**；Clip / RichRepo / LINQ 均借用 `CaseBuilder`，不另起一套字符串拼装。

### 7.2 SQLBuilder API（已实现）

```csharp
// 搜索 CASE
var flag = kit.caseWhen()
    .when("Status={0}", 1).then("待付")
    .when("Status={0}", 2).then("已付")
    .else_("关闭")
    .end("Flag");
kit.select("Id, " + flag);

// 简单 CASE
var flag2 = kit.caseOf("Status")
    .when(1).then("待付")
    .when(2).then("已付")
    .else_("关闭")
    .end("Flag");

// 便捷：直接进 SELECT
kit.selectCase(c => c.when("Status={0}", 1).then("A").else_("B"), "Flag");
kit.selectCaseOf("Status", c => c.when(1).then("A").else_("B"), "Flag");
```

文件：
- `pure/src/ado/builder/SQLKit/CaseBuilder.cs`
- `pure/src/ado/builder/StepBuilder.case.cs`
- `SQLBuilder.proxy` 转发 `caseWhen` / `caseOf` / `selectCase*`

Clip 薄封装（借用同一套）：`SQLClip.case.cs` → `Context.Builder.caseWhen()`。

IR：`CaseWord` 转译已有；补齐 `SimpleCaseWord.Accept` + `VisitSimpleCaseExpression`。

### 7.3 Window（已实现）

窗口函数同样先落 SQLBuilder（复用 `WindowOverClause` + `SQLExpression.windowOver`），再给 Clip 借。

```csharp
// 函数 + OVER
var rn = kit.window("ROW_NUMBER()")
    .partitionBy("DeptId")
    .orderBy("HireDate")
    .end("rn");
kit.select("Id, " + rn);

// 便捷
kit.selectWindow("SUM(Amount)", w => w.partitionBy("UserId").orderBy("Dt"), "Total");
kit.selectRowNumber(w => w.partitionBy("ShopId").orderByDesc("Id"), "rn");

// 仅 OVER，拼到已有表达式后
kit.select("SUM(Amt) " + kit.over().partitionBy("UserId").toOver() + " AS s");

// 帧
kit.window("SUM(Amount)")
    .partitionBy("UserId")
    .orderBy("Dt")
    .rowsBetween("UNBOUNDED PRECEDING", "CURRENT ROW")
    .end("Running");
```

文件：
- `pure/src/ado/builder/SQLKit/WindowBuilder.cs`
- `pure/src/ado/builder/StepBuilder.window.cs`
- `SQLBuilder.proxy` 转发 `window` / `over` / `selectWindow` / `selectRowNumber` / `windowRowNumber` 等

Clip 薄封装：`SQLClip.window.cs` → `Context.Builder.window(...)`。

### 7.4 任务

| 序号 | 任务 | 状态 |
|:----:|------|:----:|
| W1 | SQLBuilder CaseBuilder | ✅ |
| W2 | Clip 借用 CASE | ✅ |
| W3 | SimpleCase IR 转译 | ✅ |
| W4 | WindowBuilder @ SQLBuilder + Clip 借用 | ✅ |

---

## 8. P1 纲要（挂钩即可，本迭代可并行）

### 8.1 `useTrans` 事务糖

```csharp
db.useTrans(work =>
{
    work.useRepo<User>().Update(...);
    work.useRepo<Order>().Insert(...);
    return true; // false → 回滚
});
```

实现：包一层共享 `DBExecutor`；`work` 为绑定同一执行器的门面（`useRepo`/`useRichRepo`/`useSQL`/`useClip`）。跨库不伪装分布式事务。

路径：`pure/src/utils/door/DBQueryableExtension.cs`、`TransWork.cs`。**✅ 已落地。**

### 8.2 Schema

已并入 **§4.6**（P0）。入口：`useRichRepo<T>().EnsureSchema` / `useDDL().ensure<T>`。

### 8.3 Include / loadWith（Repo/Clip）

复用 `NavQueryGuide`：

```csharp
repo.Query().Include(x => x.Orders).ToList();
clip.from<User>().include(x => x.Orders)...
```

规则：一对多 = 二次 `IN`；禁止隐式笛卡尔；跨分片默认禁用（`NavIncludeOptions.AllowCrossShard=false`）；`MaxParentCount` / `MaxChildRows` / `MaxDepth` 可配。

```csharp
repo.Include(list, x => x.Orders, null, new NavIncludeOptions {
    MaxParentCount = 500,
    MaxDepth = 3,
    AllowCrossShard = false
});
```

### 8.4 实体级分片注册

```csharp
client.configureShard<Order>(o => { /* EntityShardConfig */ });
// 文档曾用名 configureShardFor —— 以现有 configureShard<T> 为准，不另起 API
```

与现有 `useShard` / interceptor 对齐；Repo 写入自动带 shard key。

---

## 9. 落地形态与包归属

```text
应用
  ├─ useSQL / useClip / useQueryable / useBus   ← SQL / 查询特性（CASE、Window、setCache…）
  ├─ useRepo<T>                                 ← 薄 CRUD（保持；不挂 Tracking/EntityCache/Schema/Upsert）
  ├─ useRichRepo<T>                             ← 厚业务 API 唯一落点
  └─ useTrans / useWork                         ← 事务糖（门面，非厚仓储）

pure/src/adoext/richRepo/          ← 业务厚能力代码根目录
  SooRichRepo.cs
  UpsertOptions.cs
  EntityCacheStore.cs              # 实体字典缓存内部实现
  tracking/                        # Dirty Tracking（类型 + RichRepo 消费）
  schema/                          # SchemaEnsure / SyncMode / Options
  cache/ResultCacheKey.cs          # 可选：结果缓存 key 工具（或放 ado/cache）

挂钩（可改现有，但不向薄仓暴露厚 API）：
  repository/SooRepository.cs      # 保持不动（禁止加 UpdateDirty / Track / EnsureSchema / InsertOrUpdate）
  clip/provider/EntityTranslator   # prepareUpdateMembers 等能力挂钩
  ado/DDL/DDLBuilder*.cs           # doInitTable 被 SchemaEnsure 调用
  ado/builder/…                    # CASE/Window（SQL 特性）
  clip/…                           # CASE/Window 借用（缓存不走 Clip .expire）
  utils/door/…                     # useRichRepo / useTrans / ensure<T>

内核 SQLBuilder / Dialect / DBExecutor ← 不改哲学
```

| 能力 | 归属 | 对外入口 |
|------|------|----------|
| Dirty Tracking / UpsertOptions / EntityCache / SchemaEnsure | Pure `adoext/richRepo` | **仅** `useRichRepo` / `SooRichRepo` |
| CASE / Window | `ado/builder` → Clip 借用 | `useSQL` / `useClip` |
| SELECT `setCache`（ResultCache） | `ado/cache` + SQLBuilder | `useSQL().setCache`；**不是** EntityCache |
| 薄仓 CRUD | `adoext/repository` | `useRepo`（行为不变） |

---

## 10. 实施路线（与特性计划阶段对齐）

### 阶段 A（本实施文档主战场，1～2 迭代）— ✅ 已收口

1. **T1–T6** Dirty Tracking ✅  
2. **R0–R7** ResultCache（专项计划；原 C1–C5 `.expire` **取消**）✅  
3. **B1–B3 / B5** RichRepo + EntityCache ✅  
4. **S1–S5** SchemaEnsure ✅  
5. **U1–U3** UpsertOptions ✅  

**验收**：脏更新 SQL 列可预期；字典缓存可用；EnsureSchema 默认不 DROP；Upsert 单测过；基准无回退。

### 阶段 B（查询表面）

1. W1–W4 CASE/Window（✅ 已完成；Lambda 糖可选）  
2. Include/loadWith（✅ Repo/Clip 糖复用 NavQueryGuide）  
3. Clip 全覆盖 PrintQuery / 慢 SQL（可选后置）  

### 阶段 C — ✅ 主项已落地

1. useTrans ✅（`db.useTrans`，同库 `DBExecutor`）  
2. Include/loadWith ✅  
3. configureShardFor → 文档对齐现有 `configureShard<T>`（不另起 API）✅  
4. 专题文档《业务仓储与缓存指南》✅

---

## 11. 风险与约束（实施时检查单）

- [x] 厚仓储可选；默认文档仍推 `useRepo` / `useSQL`  
- [x] **业务厚 API 不进 `SooRepository`**（Tracking / EntityCache / Schema / Upsert）  
- [x] SQL 特性（CASE/Window/`setCache`）走 Builder/Clip，不绑死 RichRepo（**不**提供 Clip `.expire`）  
- [x] Tracking：大列排除；批量走显式 set  
- [x] 富仓储未追踪 `Update` 兼容策略写清（默认全列；`UntrackedUpdateAllColumns`）  
- [x] EntityCache 与 ResultCache 文档分离；写路径失效  
- [x] 主从延迟：缓存一致性写入《业务仓储与缓存指南》  
- [x] EnsureSchema 默认不 DROP；Drop 须双闸  
- [x] AllowSchemaSync 生产可关（`configureSchema` / `DefaultAllowSchemaSync`）  
- [x] Include 深度/条数限制；跨片关闭（`NavIncludeOptions`：MaxDepth / MaxParentCount / MaxChildRows / AllowCrossShard）  
- [x] 相关单测：`SQLBuilderResultCacheTests` + RichRepo 验收集  

---

## 12. 目标用法（验收示意）

```csharp
// —— 脏更新 / Upsert / Schema / 字典缓存：一律 useRichRepo ——
var users = db.useRichRepo<User>().autoTrackOnQuery();
var user = users.GetById(1);
user.Email = "n@x.com";
users.Update(user);              // SET Email only
users.UpdateAllColumns(user);    // 全量

var dict = db.useRichRepo<SysDict>();
var name = dict.QueryFromCache(x => x.Code == "A").FirstOrDefault()?.Name;
dict.ClearCache();
dict.EnsureSchema(SyncMode.AddMissingColumns);
dict.PreviewSchema(); // 只看 SQL

db.useRichRepo<User>().InsertOrUpdate(user, new UpsertOptions
{
    ConstraintMembers = { nameof(User.UserName) },
    UpdateMembers = { nameof(User.Email), nameof(User.Status) },
    IfExistsSkipUpdate = false,
    BatchSize = 500
});

// —— 结果缓存 / CASE / Window：SQL 特性，挂 Builder（非富仓储；不提供 Clip .expire）——
var list = db.useSQL()
    .setTable("User")
    .where("Status", 1)
    .setCache(TimeSpan.FromMinutes(5))
    .queryList<User>();

var flag = db.useSQL()
    .caseWhen()
    .when("Status={0}", 1).then("待付")
    .else_("其它")
    .end("Flag");

var rn = db.useSQL()
    .windowRowNumber()
    .partitionBy("DeptId")
    .orderBy("Id")
    .end("rn");

// —— 薄仓：行为不变 ——
db.useRepo<User>().Update(user); // 全列（或不改现网语义），无 autoTrack

// —— 事务糖 ——
db.useTrans(work =>
{
    work.useRichRepo<User>().Update(user);
    work.useRepo<Order>().Insert(order);
    return true;
});
```

---

## 13. 一句话

> **SQL 特性（CASE/Window/`setCache`）落在 Builder·Clip；脏更新、字典缓存、SchemaEnsure、Upsert 只进可选独立 SooRichRepo——不污染薄仓与内核。**
