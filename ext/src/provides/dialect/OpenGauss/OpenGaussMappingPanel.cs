using mooSQL.data.Npgsql;

namespace mooSQL.data
{
    /// <summary>
    /// openGauss / GaussDB 类型映射：复用 Npgsql / PG 字面量。
    /// </summary>
    public class OpenGaussMappingPanel : NpgMappingPanel
    {
    }
}
